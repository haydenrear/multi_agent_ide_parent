# Contracts: Collector Orchestrator Routing

No new external API endpoints are required for this feature.

This feature uses existing graph events and internal routing logic. Any UI prompts for review decisions reuse existing review event mechanisms.
