{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://multi-agent-ide/schemas/regex-path-filter-policy.schema.json",
  "title": "RegexPathFilterPolicy",
  "description": "Policy for PathFilter registrations made through the regex-path endpoint. Used for raw string surfaces including prompt contributor text, controller-formatted event text, and serialized event JSON. Persisted filter JSON still uses filterType=PATH; the endpoint/discovery filter kind remains REGEX_PATH.",
  "type": "object",
  "allOf": [{ "$ref": "filter-policy.schema.json#/$defs/PolicyCommonFields" }],
  "properties": {
    "id": true,
    "name": true,
    "description": true,
    "sourcePath": true,
    "status": true,
    "priority": true,
    "isInheritable": true,
    "isPropagatedToParent": true,
    "layerBindings": true,
    "createdAt": true,
    "updatedAt": true,
    "filterType": {
      "const": "PATH",
      "description": "Persisted filter discriminator — always PATH for path filters registered through the regex-path endpoint."
    },
    "executor": {
      "$ref": "filter-policy.schema.json#/$defs/ExecutorOneOf"
    }
  },
  "required": ["filterType", "executor"],
  "additionalProperties": false
}
