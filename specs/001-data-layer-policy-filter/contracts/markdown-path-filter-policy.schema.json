{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://multi-agent-ide/schemas/markdown-path-filter-policy.schema.json",
  "title": "MarkdownPathFilterPolicy",
  "description": "Policy for PathFilter registrations made through the markdown-path endpoint. Used for heading-structured text surfaces such as prompt contributors or markdown-like controller-formatted event text. Persisted filter JSON still uses filterType=PATH; the endpoint/discovery filter kind remains MARKDOWN_PATH.",
  "type": "object",
  "allOf": [{ "$ref": "filter-policy.schema.json#/$defs/PolicyCommonFields" }],
  "properties": {
    "id": true,
    "name": true,
    "description": true,
    "sourcePath": true,
    "status": true,
    "priority": true,
    "isInheritable": true,
    "isPropagatedToParent": true,
    "layerBindings": true,
    "createdAt": true,
    "updatedAt": true,
    "filterType": {
      "const": "PATH",
      "description": "Persisted filter discriminator — always PATH for path filters registered through the markdown-path endpoint."
    },
    "executor": {
      "$ref": "filter-policy.schema.json#/$defs/ExecutorOneOf"
    }
  },
  "required": ["filterType", "executor"],
  "additionalProperties": false
}
