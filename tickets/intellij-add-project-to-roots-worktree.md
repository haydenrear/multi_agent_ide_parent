Intellij ... wow!

So in fact when we start a new worktree, we can just do something awesome - 

idea .

in the worktree directory!

Then we can just path the project path directly, and all build tools will work. What a joy! Because all libs available,
all shared indexing. Just positively genius.