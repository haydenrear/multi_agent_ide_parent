Expose Intellij instance with SSE.

Bind project container mount. 

Proxy gateway, register project. 

Filter out all other tools unnecessary such as get_file_problems.

So we can use mcp tool gateway for this one easily.
