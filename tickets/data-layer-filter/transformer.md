The transformers should be referenced in the quick-actions.

In other words, in the request for the workflow graph, it references particular transformers. This allows for us to be
able to add transformers in an ad-hoc way per request, which is what is needed to build out a repository of transformer,
and be able to learn the best transformers, and pass on information to models about what works for transformers.