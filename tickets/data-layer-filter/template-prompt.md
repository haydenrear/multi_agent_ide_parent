1. add a proxy around the template operations for each of the agent actions
2. add the ability to attach to this (propagator, filter, transformer), with the key so that it can be propagated, 
filtered, or transformed without restarting the application (for quick changes)
