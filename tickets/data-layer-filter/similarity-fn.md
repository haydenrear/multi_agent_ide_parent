Current we can use

- regex over contents
- equals over contents

However, probably there should also be a similarity function for this as well, and it can get better over time as we
gather the data.

Additionally, over time we'll be able to filter out the ones that are "OOD" and use the similarity function to filter
out or propagate them even though it didn't flag them?