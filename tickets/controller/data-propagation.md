There will be some things that will need to be intercepted and augmented, in accordance with the skill.

In fact, I've seen that the controller often misses opportunities to add to the executables folder, or forgets
outstanding.md, etc.

So this is a good one for the propagators. But this only happens once we've added the controller agent to our app.

They will be, for each of these things in the skills, propagators, that say, effectively (matching over particular
tool calls, for instance), can you check to see, given this skill directory and this tool call, whether or not the 
model has skipped the opportunity to refine or add a script there, etc. Using it with intelligence (and this is one
for small models), will be helpful and make sure we adhere to our practices. 

I also like the idea of using a semantic embedding with similarity function to determine whether to run a propagator for
these cases - i.e. checking closeness to other ones that definitely are.