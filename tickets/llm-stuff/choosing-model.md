Model and ACP provider should be chosen dynamically using an agent/controller/llm.

We should provide:

1. current usage for all acp providers
2. context

And then it should make tradeoffs deciding what type of model to use.