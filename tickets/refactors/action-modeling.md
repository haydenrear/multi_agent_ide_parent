We should have

interface MultiAgentIdeAction

which is a bean, and contains all the information that needs to be registered, in places like FilterLayerCatalog, such
as action name, etc.

It's mostly there to run an ActionDescriptor data record.