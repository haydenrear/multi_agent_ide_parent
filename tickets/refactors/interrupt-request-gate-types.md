The permission gate types are a bit disorganized and messed up.

There should probably be a sealed interface InterruptNode with discrete records for them. 

Moreover, the listener should probably match over the types of them. This gives a better understanding of what's 
happening. Currently, the "review" interrupt is pretty much BS. It was generated by AI in a very disorganized way,
the InterruptService - and needs to be better modeled.