Create an implementation of dev.langchain4j.model.chat.ChatModel that uses agentclientprotocol https://github.com/agentclientprotocol/kotlin-sdk/blob/master/samples/kotlin-acp-client-sample which has a kotlin SDK - and can therefore be used with Java.

So we'll be starting to provide codex implementation so that we can run with codex.