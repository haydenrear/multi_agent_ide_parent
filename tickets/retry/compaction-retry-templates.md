There can be a number of exceptions to be added to the blackboard history for which we can skip adding any prompt contributor. If, for example, the blackboard history added the same request twice in a row, with only expceptions in between, then we can be pretty sure that this is a retry. We probably still want to include the json schema in this case, however we can remove the prompt contributors, and potentially have a different jinja template for each of them, if it's a retry.

So then it is better to save the CompactionEvent if it's detected to the BlackboardHistory, and then skip adding a lot of the prompt contributors. And if it's a second retry with compaction, say, skip all prompt contributors and have retry jinja template, say, but on the first we just skip some prompt contributors and have the same jinja template.

We could add to prompt contributor SkipCompaction interface, skipping all prompt contributors if we detect it's a compaction. And in the agent executor match to see if it's a retry type and use a different template that only contains the stuff that's absolutely necessary. Mostly because we need to make sure and write the json schema again.

The blackboard history subscribes to the events, so just write a CompactionRequest to the blackboard history. And we add a listener for onError, which loads the blackboard history and adds the error. So then on BlackboardHistory, we have a method that says compactionStatus, which says, FIRST, or MULTIPLE. And another that returns an errorType, which has a sealed interface, one of which is NoError, and then we add various types of errors which we match over in the agent executor to determine the model we add and the template. And then in our args we have templates for each action for compaction, and the different types of errors we want to handle. And we only have one arg for this which is ErrorTemplates.

That's for the DefaultLlmRunner and the AgentExecutor, however we also want to be able to specify for PromptContributor. Therefore we'll want to pass an error descriptor into the prompt context and the decorator context which we can match over and add methods for to tune the creation of the request, result, and most importantly the prompt contributors.