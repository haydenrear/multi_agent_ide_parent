There should be a prompt loaded every time make changes, plan, ticket, have anything to do with nodeId, sessionId,
artifact key, that any time we set this, we should be thinking of the hierarchy. Again it made a mistake and didn't
ask.

when using events the nodeId should be a child of the current request if possible.